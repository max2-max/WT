<?php
$uname=$_GET['u'];
$pass=$_GET['p'];
$conn=pg_connect("host=localhost dbname=practice_db user=postgres password=postgres");
$query="select * from users where username='$uname' and password='$pass'";$result=pg_query($conn,$query);
if(pg_num_rows($result)>0)
{
echo "Valid User";
}
else
{
echo "Invalid Username or Password";
}
?>
